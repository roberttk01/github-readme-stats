name: Update GitHub Contributions Image

on:
  schedule:
    - cron: "0 0 * * *"  # Runs daily at midnight UTC
  push:
    branches:
      - master

jobs:
  update-image:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Repository
        uses: actions/checkout@v3

      # Step 2: Generate the GitHub contribution snake image (SVG)
      - name: Generate GitHub Contribution Snake Image
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: dist/github-contributions.svg

      # Step 3: Install rsvg-convert and ImageMagick
      - name: Install rsvg-convert and ImageMagick
        run: |
          sudo apt-get update
          sudo apt-get install -y librsvg2-bin imagemagick

      # Step 4: Convert SVG to PNG, then PNG to GIF
      - name: Convert SVG to PNG and then to GIF
        run: |
          rsvg-convert dist/github-contributions.svg -o dist/github-contributions.png
          convert dist/github-contributions.png dist/github-contributions.gif

      # Step 5: Commit and Push Changes
      - name: Commit and Push Changes
        run: |
          git config --local user.email "roberttk01@gmail.com"
          git config --local user.name "GitHub Actions"
          git add dist/
          git commit -m "Update GitHub contributions image (GIF)" || echo "No changes to commit"
          git push
